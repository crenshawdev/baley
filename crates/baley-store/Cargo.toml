[package]
name = "baley-store"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "The storage port of the Baley evidence ledger: the traits an engine adapter implements, and the conformance suite every adapter passes."
repository = "https://github.com/crenshawdev/baley"
rust-version = "1.98"
publish = false

# No dependencies yet. This crate must never learn which engine is behind it
# (design 0001, EVD-R12); CI checks that baley-core cannot reach rusqlite
# through it.
[dependencies]
